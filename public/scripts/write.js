const form = document.querySelector(".write-container");
const btnAdd = document.querySelector(".btn-add");

function formWrite() {}

function writeAdd() {}

btnAdd.addEventListener("click", writeAdd);
form.addEventListener("submit", formWrite);
